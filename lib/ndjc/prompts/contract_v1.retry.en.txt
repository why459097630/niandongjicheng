Fix the following violations and RETURN STRICT JSON with the SAME KEYS and NESTING as SKELETON:

{ISSUES}

RE-CHECK ALL HARD RULES:
- OUTPUT SHAPE
  • One JSON object only; no prose/markdown/comments.
  • Top-level keys EXACTLY: "metadata", "anchorsGrouped", "files".
  • Inside "anchorsGrouped": "text", "block", "list", "if", "hook", "gradle".
  • Mirror SKELETON exactly; no added/removed/renamed/reordered keys.

- COMPLETENESS & NO EMPTIES
  • Every anchor in SKELETON MUST be filled.
  • Forbid "", [], {}, null, "N/A", "default", "tbd", "-".
  • Unknown values → placeholders:
    - Strings: "__NDJC_PLACEHOLDER__"
    - Lists: ["__NDJC_PLACEHOLDER__"]
    - Booleans: true/false (native)
    - Gradle arrays (resConfigs, permissions): ["__NDJC_PLACEHOLDER__"] (NEVER empty)

- TYPE ENFORCEMENT
  • Booleans/numbers MUST NOT be strings. Example fixes: "false"→false, "300"→300.

- ANDROID BUILD
  • applicationId regex: ^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)+$
  • resConfigs item regex: ^[a-z]{2}(-r[A-Z]{2})?$ and array MUST be non-empty
  • permissions allowlist:
    ^(android\.permission\.(INTERNET|ACCESS_NETWORK_STATE|READ_MEDIA_IMAGES|READ_MEDIA_VIDEO|READ_MEDIA_AUDIO|POST_NOTIFICATIONS|CAMERA|ACCESS_FINE_LOCATION))$
    (Invalid/legacy → replace with placeholder list, not [])

- THEME_COLORS
  • Must be an OBJECT: {"primary":"#RRGGBB","secondary":"#RRGGBB"} (do NOT double-escape)

- BLOCK/HOOK SAFETY
  • Kotlin/Compose/Gradle/XML-safe; balanced quotes/brackets; no '<' or '>' in strings.

- GLOBAL
  • Do NOT change metadata; do NOT add new anchors.

Return ONLY the corrected JSON object.
