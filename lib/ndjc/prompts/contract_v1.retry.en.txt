Your previous reply violated the contract. Fix ALL issues and resend the **entire JSON** with the SAME key set as the SKELETON.

Violations:
{{VIOLATIONS}}

Non-negotiable rules to satisfy now:
1) Return **pure JSON** (no Markdown fences, no comments, no prose).
2) Use **canonical keys only**; do not rename or add/remove any keys.
3) Blocks/Hooks MUST be directly-injectable code/XML or raw JSON (NOT escaped JSON; NOT explanations).
4) `files` MUST be present, with **path/kind/overwrite/content** for each item and **files.length ≥ 2**.
   - Include at least:
     - app/src/main/res/raw/theme_colors.json  (kind=json, overwrite=true, with a small JSON object)
     - app/src/main/res/drawable/ic_stat_notify.xml (kind=xml, overwrite=true, valid VectorDrawable)
5) Gradle/Kotlin hooks should contain plausible code (e.g., `dependencies {}`, `plugins {}`, `tasks.register(...)`).
6) Ensure the JSON parses with JSON.parse without any post-processing.

Resend the full corrected JSON now.
