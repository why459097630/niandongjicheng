Your previous output contains issues:

{ISSUES}

Please fix them and return STRICT JSON only with the SAME keys and nesting as the given SKELETON.

Absolute requirements:
- No placeholders or templates (deny: ^__.*__$, {{.*}}, placeholder/todo/sample/example/模板/样例).
- No empty values: "", [], {}, null.
- All anchors must be compilable for a native Android APK.

Group-specific checks to pass:
- block: length >= 30 AND must contain at least one of: "@Composable", "fun ", "LazyColumn", "Modifier."
- list: every item length >= 2
- hook: length >= 10
- gradle.applicationId: ^[a-z][a-z0-9_]*(\.[a-z][a-z0-9_]*)+$
- gradle.resConfigs[*]: ^[a-z]{2}(-r[A-Z]{2})?$
- gradle.permissions[*]: use valid Android permissions such as:
  - android.permission.INTERNET
  - android.permission.ACCESS_NETWORK_STATE

Examples:
GOOD block: "@Composable fun MenuList() { LazyColumn { /* ... */ } }"
BAD  block: "__FOO__", "{{BAR}}", "todo"

GOOD list: ["home", "menu", "detail"]
BAD  list: []

GOOD gradle.applicationId: "com.ndjc.demo.food"
BAD  gradle.applicationId: "__APP__", "com..bad"

Return ONLY the strict JSON. Do not add or remove keys. Do not include explanations.
