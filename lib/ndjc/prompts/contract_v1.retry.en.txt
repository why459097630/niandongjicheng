The previous JSON did not fully comply.

Please FIX the following and RETURN FULL JSON again (same key-set and structure as SKELETON):

Missing required keys/values:
- {{missing}}

Bad or placeholder formats:
- {{bad}}

Rules reminder:
- Use ONLY canonical keys; do not add/remove/rename keys.
- text: non-empty, non-placeholder strings.
- list: array of non-placeholder strings (≥1 item if required).
- if: booleans only.
- block/hook: non-placeholder strings (code/XML/plain allowed).
- gradle: correct types (e.g. applicationId string; resConfigs[], permissions[]).
- Use provided defaults when info is missing.
- Output STRICT JSON only (no prose, no code fences).
