The previous JSON violates the NDJC Contract v1.

Detected issues:
{{ISSUES}}

You must FIX ALL issues and RETURN THE FULL JSON AGAIN.

ABSOLUTE RULES
- Keep EXACT SAME key set as the SKELETON (no add/remove/rename).
- Use ONLY canonical keys (no aliases, no extras).
- Use defaults where data is missing. Do NOT omit required keys.
- Output RAW JSON ONLY (no Markdown, no code fences, no comments).

STRICT TYPE & FORMAT
- anchors.text: string
- anchors.block: string (Kotlin/XML/Gradle snippet), NO code fences/backticks
- anchors.list: string[] (array of strings only)
- anchors.if: boolean (true/false literals only)
- anchors.hook: string (no fences)
- anchors.gradle: { applicationId: string; resConfigs?: string[]; permissions?: string[] }
- files: path (project-relative, no "../"), content (string, no fences), overwrite?: boolean, kind?: "kotlin"|"xml"|"json"|"md"|"txt"

DO NOT return a diff or partial fragment — return the FULL corrected JSON object now.
